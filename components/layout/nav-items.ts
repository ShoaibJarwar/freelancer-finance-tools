// Only routes that exist today. Add Tools/Guides/About here once those
// pages are actually created in later phases — do not link ahead of them.
export const navItems = [{ href: "/", label: "Home" }] as const;
